add_executable(
        ${PROJECT_NAME}
        main.c
        parser.c
        node.c
        token-iterator.c
        lexer.c
        shell-tokens.c
        token.c
        string-iterator.c
)
target_include_directories(
        "${PROJECT_NAME}"
        PRIVATE
        "${PROJECT_SOURCE_DIR}/include"
)